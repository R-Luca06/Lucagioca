# Normalise les fins de ligne : LF dans le repo, quel que soit l'OS du contributeur.
* text=auto eol=lf

# Fichiers binaires — jamais de conversion.
*.png binary
*.jpg binary
*.webp binary
*.avif binary
*.woff binary
*.woff2 binary
*.mp3 binary
*.ogg binary
*.mp4 binary

# Généré : masqué dans les diffs GitHub.
pnpm-lock.yaml linguist-generated=true -diff
